

export type ParamList = {
  Main : undefined,
  Search : undefined,
  Videos : undefined,
  Shopping : undefined,
  Profile : undefined,
  Test : undefined
}

export type Test ={
  Main : undefined,
  Subscreen : undefined
}

export type IconTab = {
  focused: boolean; 
  color: string;
};